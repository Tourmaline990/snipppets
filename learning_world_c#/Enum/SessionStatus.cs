public enum InitialState
{
    _drafted,
    _launched,
    _cancelled
}

public enum DispositionState
{
    _admitting,
    _locked,
    _paused,
    _available
}
public enum ActiveStatus
{
    _ongoing,
    _completed,
    _paused,
    _inActive
}